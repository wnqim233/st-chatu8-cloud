{
  "name": "st-chatu8-cloud",
  "version": "3.1.0-cloud.6",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "package": "python3 scripts/package.py",
    "preview": "node scripts/preview.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jquery": "^3.7.1"
  }
}
